# Published synthetic tenant QA

Prepared only. Run only after orchestration verifies the completed import and published deployment are ready; the user has already authorized this QA.

Michael authorized his exact controlled mailbox michael@5central.capital for three clearly labeled synthetic QA emails: application resume, invitation, and password reset. Use no alias and no other recipient. Read each actually received message privately. This is the preferred public-flow path; do not use the no-send fixture alternative as proof of delivery. `runPublishedApplicationQa` prepares the actual public submission and approval/conversion sequence; the token callback pauses for the actual received application link.

Use the published HTTPS origin and an existing authorized manager session. Keep session cookie and CSRF in process memory. Caller-created property/unit IDs begin `qa-`; visible names begin `QA TEST`. Application conversion generates its own application/person/tenancy IDs: retain the exact returned IDs and verify their synthetic relationships. Public delivery uses only michael@5central.capital; `qa-<run>@qa.example.test` is reserved for the separate no-send helper. Never select an imported tenant or real property/unit for write tests. Create a separate QA TEST property and two QA TEST units. The public application unit must be ready and temporarily listed under an active QA property with complete address facts; verify it appears in public application options. Keep the second unit unlisted, and archive/cancel the synthetic records after verification; retain their append-only audit trail. Do not alter server time.

1. Read `/api/rent-ops/auth/session` and `/api/rent-ops/auth/csrf`; verify manager role. Read dashboard, tenants, applications and reports using explicit actual local date/month. Do not save returned real data into the test artifact.
2. Exercise the actual public start/resume/certify/submit path using the authorized controlled mailbox above. Start requires configured delivery and never returns a resume token. Read the actual received link privately and pass its token in memory. Never replace the production notifier or expose a debug token route. A fixture-only flow is not proof of published delivery.
3. Use actual current date for submission/review/approval. Conversion currently requires tomorrow or later for planned move-in. Verify that future tenancy with exact synthetic unit/person/lease/schedule. To test an actual move-in today, record an explicit QA-only correction to planned date and lease start today through revision-checked manager PATCH, then record current status and actualMoveInOn today. Do not set future actual move-in or advance production time. This tests a corrected plan, not elapsed time.
4. Read-only billing preview is `GET /api/rent-ops/billing/preview?month=YYYY-MM`. Posting is `POST /api/rent-ops/billing/post` with `{month, previewToken}` and applies globally to the month; there is no tenancy/property scope. Do not invoke that POST against the imported production portfolio. Preview may be inspected without saving real data. Posting/payment execution remains confined to the isolated synthetic environment; this published runner makes no checkout/webhook calls.
5. Upload the synthetic lease through authenticated `POST /api/rent-ops/tenancies/:id/lease-files`, using the exact synthetic tenancy ID, manager session and `x-rent-ops-csrf`, raw PDF bytes with `Content-Type: application/pdf` and `x-document-name`. Do not submit JSON or client-supplied scope headers/query fields. The server resolves the person/property/unit from the tenancy and persists the verified object binding. Require HTTP 201, then verify protected tenant viewing/download, unchanged source bytes/hash/size, and cross-tenant denial. No tenant upload shortcut is needed.
6. `runProductionTenantQa` from tenant-production-qa.ts runs no-send creation, activation/replay denial, tenant/admin scope, own PDF, password change, admin no-send reissue/activation, and revokes only its created account in finally. Caller supplies exact confirmed synthetic identities; it rejects imported/non-QA prefixes. No credential output or files. This separate no-send helper does not accept conversion-generated IDs and does not establish invitation/reset email delivery. The public workflow must separately exercise those two authorized messages and their received links.
7. Browser: manager dashboard, application detail/conversion, named QA resident, reports; tenant fresh login and in-portal lease canvas +accessible text/zoom. Never change browser security, inspect privileged download pages, or use real payment details.

Required inputs: published origin; existing authorized manager session; confirmed QA property/two unit IDs; synthetic application token through the exact controlled mailbox; synthetic PDF bytes. Live execution waits for verified import/deployment readiness, not a new user permission request. Revoke QA accounts, cancel QA tenancies and unlist/archive QA units/property using normal audited routes after testing.
